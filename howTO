event:
	event type:
		internal
		external
quantumNetwork:
queue: (each item in queue is an event with some properties)
	while(queue not empty)
		event = pop the first event from stack
		switch event.type
		case internal:
			handleInternal(event)
		case external:
			handleExternal(event)
		case default:
			throw new Exception()

